# ── Data files (large / binary) ──────────────────────────────────
Data/

# ── Archive (old code, pre-restructure) ─────────────────────────
_archive/

# ── Personal notes (report drafts, not part of the codebase) ────
notes/

# ── Interview recordings and transcripts ────────────────────────
# Named recordings and transcripts of identifiable participants. The
# thesis reports them anonymised (R1-R8); this repo is public, so the
# raw material must never be tracked. Keep the files on disk, not in git.
11_Interviews/

# ── Presentations / slide decks (not part of the codebase) ──────
*.pptx

# ── Legacy folders (kept for reference, gitignored) ─────────────
Developing/
Testing/

# ── Common data formats ─────────────────────────────────────────
*.ply
*.las
*.laz
*.gml
*.GML
*.gpkg
*.dxf
*.qgz
*.qml
*.sld
*.zip

# ── Python cache ─────────────────────────────────────────────────
__pycache__/
*.pyc
*.pyo

# ── Claude Code config ──────────────────────────────────────────
.claude/
CLAUDE.md

# ── Secrets ──────────────────────────────────────────────────────
.env
*api*key*.env

# ── OS junk ──────────────────────────────────────────────────────
Thumbs.db
.DS_Store
*.tex
make_ler_appendix.py
*.docx
/figures

# ── Active site selection (per-machine, see its docstring) ──────
core/site_local.py
/tools
*.png
*.json
